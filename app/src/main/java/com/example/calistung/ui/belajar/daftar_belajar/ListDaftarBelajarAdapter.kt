package com.example.calistung.ui.belajar.daftar_belajar


/*
class ListDaftarBelajarAdapter(private val listUser: ArrayList<Any>) :
    RecyclerView.Adapter<ListDaftarBelajarAdapter.ListViewHolder>() {


    class ListViewHolder(private var binding: ItemIsiBelajarBinding) :
        RecyclerView.ViewHolder(binding.root) {
        fun bind(listUser: Any) {
            with(binding) {

            }


        }

        override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): ListViewHolder {
            val itemIsiBelajarBinding =
                ItemIsiBelajarBinding.inflate(LayoutInflater.from(parent.context), parent, false)
            return ListViewHolder(itemIsiBelajarBinding)
        }

        override fun onBindViewHolder(holder: ListViewHolder, position: Int) {
            val user = listUser[position]
        holder.bind(user)

        }

        override fun getItemCount(): Int = listUser.size


    }
}*/
